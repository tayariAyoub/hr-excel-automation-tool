# Generated Excel files

Run the following command from the project folder:

```bash
python create_sample_data.py
```

The application will create:

- `employees.xlsx`
- `working_hours.xlsx`
- `monthly_report.xlsx`

These generated workbooks are intentionally not committed because each company
will use its own private employee and payroll data.
